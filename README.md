# Car MPG Prediction API

Machine learning API built with FastAPI, Docker, and PostgreSQL.

## Features

Predict car MPG
Retrain ML model
Health check endpoint

## Endpoints

POST /predict
POST /retrain
GET /health

## Run with Docker

docker-compose up --build
